package ufsic.scheme.patterns;

import ufsic.scheme.Scheme;

public class EmailDefaultPattern extends HtmlPattern {

  public EmailDefaultPattern(Scheme scheme, boolean autoLoad) {
    super(scheme,autoLoad);
  }
  
  public EmailDefaultPattern(Scheme scheme) {
    super(scheme);
  }
  
}
