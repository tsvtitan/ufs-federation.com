package ufsic.scheme.patterns;

import ufsic.scheme.Scheme;

public class SmsDefaultPattern extends PlainPattern {

  public SmsDefaultPattern(Scheme scheme, boolean autoLoad) {
    super(scheme,autoLoad);
  }
 
  public SmsDefaultPattern(Scheme scheme) {
    super(scheme);
  }
  
}
